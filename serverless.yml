service: just-scored

provider:
  name: aws
  runtime: go1.x
  stage: dev
  region: us-east-1

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  goal-checker:
    handler: bin/goal-checker
    events:
      # Every minute 12am to 2pm Moscow time (UTC+3)
      - schedule: cron(* 9-23 * * ? *)
