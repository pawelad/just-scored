service: just-scored

provider:
  name: aws
  runtime: go1.x
  stage: dev
  region: us-east-1

package:
 exclude:
   - ./**
 include:
   - ./bin/**

functions:
  goal-checker:
    handler: bin/goal-checker
    events:
      - schedule: rate(1 minute)
